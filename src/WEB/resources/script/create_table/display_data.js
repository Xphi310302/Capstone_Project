// const imageCanvas = document.getElementById('imageCanvas');

// // Function to resize the canvas to fit the window size
// function resizeCanvas() {
//     // Set canvas dimensions to match the window size
//     imageCanvas.width = window.innerWidth;
//     imageCanvas.height = window.innerHeight;
// }

// // Call the resizeCanvas function when the window is resized
// window.addEventListener('resize', resizeCanvas);

// // Call the resizeCanvas function initially to set canvas dimensions
// resizeCanvas();